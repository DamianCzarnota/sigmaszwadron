# sigmaszwadron
Project for cloudpgm and mobiapps

# Run batch job locally

```
python3 ./application.py <json-file-with-firebase-credentials> <bucket-name> <realtimedatabase-address> <nasa-api-key>
```